# Infrastructure

Cloud deployment manifests, container orchestrations, and Infrastructure as Code (IaC).

## Structure
- `docker/`: Base container configurations, multi-stage production Dockerfiles.
- `k8s/`: Kubernetes manifests (deployments, services, ingress, configmaps).
- `terraform/`: Cloud provisioning blueprints (VPC, Managed Postgres, Redis, Container Registry).
